package br.github.superteits.brasille.storage;

public class BrasilleStatusDAO {
	
	/*public void saveMap() {
		
	}
	
	public void createUser(Player player) {
		String stmt = "INSERT INTO PlayerStatus" + 
							" (uuid, str, vit, dex, agi, inte, fp, canChangeLvl, critChance, flee)" +
							" VALUES (?,?,?,?,?,?,?,?,?,?)";
		Status status = new Status(player.getUniqueId().toString(), 0, 0, 0, 0, 0, 0, true, 0, 0);
		
		PreparedStatement ps = Brasille.con.prepareStatement(stmt);
		//PreparedStatemant ps = ConnectionFactory.getConnection().pre
	}*/

}
